<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/WarmBeige"
    tools:context=".PickUpConfirmationActivity">

    <!-- Confirmation Message -->
    <TextView
        android:id="@+id/txtConfirmationMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pick-Up Scheduled Successfully!"
        android:textSize="20sp"
        android:textColor="@color/charcoalGray"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginTop="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Display Pick-Up Details -->
    <TextView
        android:id="@+id/txtPickupDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pick-Up Details:"
        android:textSize="16sp"
        android:textColor="@color/charcoalGray"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/txtConfirmationMessage"
        app:layout_constraintStart_toStartOf="parent" />


    <TextView
        android:id="@+id/txtItems"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Items will appear here..."
        android:textSize="14sp"
        android:textColor="@color/charcoalGray"
        android:padding="8dp"
        android:layout_marginTop="12dp"

        app:layout_constraintTop_toBottomOf="@id/txtPickupDetails"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnBackToHome"/>


    <!-- Home Button -->
    <Button
        android:id="@+id/btnBackToHome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back to Home"
        android:textColor="@color/softCreamWhite"
        android:backgroundTint="@color/ForestGreen"
        android:layout_marginTop="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
